const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    extend: {},
  },
  variants: {
    extend: {
      borderColor: ['focus-visible'],
      opacity: ['disabled'],
      textColor: ['visited']
    }
  }
}
